apiVersion: v1
kind: Service
metadata:
  name: omnisolve-service
  labels:
    app: omnisolve-chatbot
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: omnisolve-chatbot
